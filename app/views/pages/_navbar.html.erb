<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<%= link_to "Avion Blog App", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
				<%= link_to "All Articles", articles_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "My Articles", my_articles_path(current_user), class: "nav-link" if current_user %>
      </li>
      <li class="nav-item">
        <%= link_to "Profile", profile_page_path(current_user), class: "nav-link" if current_user %>
      </li>
      
    </ul>

    <div class="ml-auto" ><%= current_user.email if current_user %></div>
    <%= link_to "Login", new_user_session_path, class: "ml-auto" if current_user.nil?%>
    <%= link_to "Logout", destroy_user_session_path, class: "ml-3", method: :delete if current_user %>
  </div>

</nav>